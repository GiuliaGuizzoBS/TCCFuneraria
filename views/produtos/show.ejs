<head>
  <style>
    body {
      background-color: #d9c8b8;
      margin: 0;
      padding: 0;
    }

    /* ===== CONTAINER ===== */
    .detalhes-container {
      background-color: #fff8eb;
      border: 1px solid #d9c8b8;
      border-radius: 15px;
      padding: 1.8rem;
      margin: 2.5rem auto;
      max-width: 550px; /* tamanho intermediário */
      box-shadow: 0 2px 10px rgba(87, 51, 15, 0.15);
    }

    .titulo-centralizado {
      text-align: center;
      color: #57330f;
      margin-bottom: 1.5rem;
      font-size: 1.6rem;
    }

    p {
      color: #57330f;
      font-size: 1rem;
      margin-bottom: 0.6rem;
      line-height: 1.5;
    }

    strong {
      color: #6a3d13;
    }

    /* ===== IMAGEM (AGORA MENOR) ===== */
    .produto-imagem {
      display: block;
      width: 200px;        /* imagem menor e proporcional */
      height: 200px;       /* quadrada */
      object-fit: cover;   /* sem distorcer */
      margin: 1rem auto;
      border-radius: 10px;
      border: 1px solid #d9c8b8;
      box-shadow: 0 1px 6px rgba(87, 51, 15, 0.1);
    }

    /* ===== BOTÕES ===== */
    .btn-marrom, .btn-danger {
      border: none;
      border-radius: 8px;
      padding: 0.6rem 1.2rem;
      font-weight: 500;
      text-decoration: none;
      min-width: 110px;
      text-align: center;
      transition: background-color 0.2s, transform 0.1s;
      margin: 0.3rem;
      display: inline-block;
    }

    .btn-marrom {
      background-color: #57330f;
      color: #fff;
    }

    .btn-marrom:hover {
      background-color: #6a3d13;
      transform: scale(1.03);
    }

    .btn-danger {
      background-color: #b54a3a;
      color: #fff;
    }

    .btn-danger:hover {
      background-color: #99372a;
      transform: scale(1.03);
    }

    .text-center {
      text-align: center;
    }

    form {
      display: inline;
    }

    /* ===== RESPONSIVIDADE ===== */
    @media (max-width: 768px) {
      .detalhes-container {
        padding: 1.2rem;
        margin: 1.5rem;
        max-width: 90%;
      }

      .produto-imagem {
        width: 180px;
        height: 180px;
      }

      .titulo-centralizado {
        font-size: 1.3rem;
      }
    }

    @media (max-width: 480px) {
      p {
        font-size: 0.95rem;
      }

      .produto-imagem {
        width: 150px;
        height: 150px;
      }

      .btn-marrom, .btn-danger {
        padding: 0.5rem;
        font-size: 0.9rem;
      }
    }
  </style>
</head>

<%- include('../partials/navbar') %>

<div class="detalhes-container">
  <h2 class="titulo-centralizado"><strong>Detalhes do Produto</strong></h2>

  <% if (produto.imagem) { %>
    <img src="<%= produto.imagem ?? '/img/placeholder.jpg' %>" alt="<%= produto.nome %>" class="produto-imagem">
  <% } %>

  <p><strong>Nome:</strong> <%= produto.nome %></p>
  <p><strong>Descrição:</strong> <%= produto.descricao %></p>
  <p><strong>Preço:</strong> R$ <%= Number(produto.preco).toFixed(2).replace('.', ',') %></p>
  <p><strong>Quantidade:</strong> <%= produto.quantidade %></p>

  <div class="text-center mt-4">
    <a href="/produtos" class="btn-marrom">Voltar</a>
    <a href="/produtos/<%= produto.id %>/edit" class="btn-marrom">Editar</a>
    <form action="/produtos/<%= produto.id %>?_method=DELETE" method="POST">
      <button type="submit" class="btn-danger">Excluir</button>
    </form>
  </div>
</div>
